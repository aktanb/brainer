var Marginal;
(function (Marginal) {
    var Strings = (function () {
        function Strings() {
        }
        Strings.loading = $('#MA_Strings').find('.loading-string').text();
        Strings.answeredRight = $('#MA_Strings').find('.loading-string').text();
        Strings.stopPlaying = $('#MA_Strings').find('.loading-string').text();
        Strings.iDontKnow = $('#MA_Strings').find('.loading-string').text();
        Strings.continue = $('#MA_Strings').find('.loading-string').text();
        return Strings;
    })();
    Marginal.Strings = Strings;
})(Marginal || (Marginal = {}));
//# sourceMappingURL=Marginal.Strings.js.map